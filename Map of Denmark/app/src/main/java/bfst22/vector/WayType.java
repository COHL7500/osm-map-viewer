package bfst22.vector;

// A list of all the types we draw.
// TODO: Add a sufficient amount of types to paint/draw the entire map.

/** "UNKNOWN" is temporary and covers all types that has not yet been accounted for.
 Ultimately, we would have accounted for all the necessary types.
 **/

public enum WayType {
    WATER, BUILDING, UNKNOWN;
}
